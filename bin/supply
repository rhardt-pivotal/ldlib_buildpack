#!/bin/bash
set -e

echo "IN SUPPLY!!"
pwd
ls -al

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
echo $DIR


mkdir $3/$4/lib

echo "$3/$4/lib"

cp $DIR/../libsrc/* $3/$4/lib
cp $DIR/../config.yml $3/$4
